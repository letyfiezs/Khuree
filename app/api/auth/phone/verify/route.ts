import { createSupabaseServerClient } from "@/lib/supabase";
export async function POST(request:Request){const body=await request.json() as {phone?:string;code?:string;returnTo?:string};if(!body.phone||!/^\+976\d{8}$/.test(body.phone)||!body.code||!/^\d{6}$/.test(body.code))return Response.json({error:"Дугаар эсвэл 6 оронтой код буруу байна."},{status:400});const supabase=await createSupabaseServerClient();const {data,error}=await supabase.auth.verifyOtp({phone:body.phone,token:body.code,type:"sms"});if(error||!data.user)return Response.json({error:"Код буруу эсвэл хугацаа нь дууссан байна."},{status:401});const safeReturn=body.returnTo?.startsWith("/")&&!body.returnTo.startsWith("//")?body.returnTo:"/movies";return Response.json({ok:true,returnTo:safeReturn});}
