import {SiteHeader} from '@/components/site-header';import {CatalogGrid} from '@/components/catalog-grid';import {getCatalog} from '@/lib/catalog';import {requireUser} from '@/lib/auth/local-auth';
export const dynamic='force-dynamic';export default async function SeriesPage(){await requireUser('/series');const items=getCatalog('series');return <main><SiteHeader/><section className="catalog-page"><div className="catalog-banner series-banner"><p className="section-kicker">АНГИ БҮР ШИНЭ ТҮҮХ</p><h1>Олон ангит</h1><p>Дараагийн ангийг нь хүлээлгэх хамгийн сонирхолтой цувралууд.</p></div><div className="catalog-body"><div className="catalog-title"><h2>Бүх цуврал</h2><span>{items.length} бүтээл</span></div><CatalogGrid items={items}/></div></section></main>}
