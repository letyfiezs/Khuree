import Link from 'next/link';import type {ContentItem} from '@/lib/content';
export function CatalogGrid({items}:{items:ContentItem[]}){return <div className="catalog-grid">{items.map(item=><Link href={`/movie/${item.slug}`} className="film-card" key={item.id}><div className="poster" style={{background:`radial-gradient(circle at 55% 25%,${item.accent}cc,transparent 40%),linear-gradient(155deg,${item.accent},#050505)`}}><span className="poster-mark">ХҮРЭЭ</span><span className="poster-title">{item.title}</span><span className="film-tag">{item.kind==='series'?'ЦУВРАЛ':item.videoKey?'ШИНЭ':'HD'}</span><span className="play-chip">▶</span></div><h3>{item.title}</h3><p>{item.year} • {item.duration} • {item.genre.slice(0,2).join(', ')}</p></Link>)}</div>}
