'use client';
import {useState} from 'react';import type {ContentItem} from '@/lib/content';
export function AdminMovies({initial}: {initial:ContentItem[]}){const [items,setItems]=useState(initial);const [open,setOpen]=useState(false);const [title,setTitle]=useState('');function add(e:React.FormEvent){e.preventDefault();if(!title.trim())return;setItems(v=>[{id:crypto.randomUUID(),slug:title.toLowerCase().replaceAll(' ','-'),title,synopsis:'Шинэ demo бүтээл',year:2026,duration:'—',age:'13+',rating:0,genre:['Драма'],kind:'movie',status:'draft',accent:'#581018'},...v]);setTitle('');setOpen(false)}return <><div className="admin-toolbar"><div><h1>Кино</h1><p>{items.length} бүтээл · каталогийн удирдлага</p></div><button className="primary-button" onClick={()=>setOpen(true)}>＋ Кино нэмэх</button></div><div className="admin-table"><div className="table-head"><span>БҮТЭЭЛ</span><span>ТӨРӨЛ</span><span>ТӨЛӨВ</span><span>ОН</span><span/></div>{items.map(i=><div className="table-row" key={i.id}><div className="title-cell"><i style={{background:i.accent}}/ ><span><b>{i.title}</b><small>{i.slug}</small></span></div><span>{i.kind==='movie'?'Кино':'Цуврал'}</span><span className={`status ${i.status}`}>{i.status==='published'?'Нийтэлсэн':'Ноорог'}</span><span>{i.year}</span><button aria-label="Үйлдэл">•••</button></div>)}</div>{open&&<div className="modal-backdrop"><form className="admin-modal" onSubmit={add}><div className="modal-title"><h2>Шинэ кино</h2><button type="button" onClick={()=>setOpen(false)}>×</button></div><label>Киноны нэр<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="Жишээ: Алсын бараа"/></label><label>Төрөл<select><option>Бүрэн хэмжээний кино</option><option>Олон ангит</option></select></label><label>Тайлбар<textarea placeholder="Товч агуулга..."/></label><div className="modal-actions"><button type="button" onClick={()=>setOpen(false)}>Болих</button><button className="primary-button">Ноорог үүсгэх</button></div></form></div>}</>}
